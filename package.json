{
  "name": "calculator",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "sass src/style.scss src/style.css",
    "dev-sass": "sass src/style.scss src/style.css --watch",
    "dev": "npm run dev-sass && http-server"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "http-server": "^14.1.1",
    "sass": "^1.56.1"
  },
  "devDependencies": {
    "typescript": "^4.9.3"
  }
}
